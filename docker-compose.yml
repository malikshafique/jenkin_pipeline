version: '3.4'

services:
  demoproject:
    image: demoproject
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 5160:5160
